# Implementation Plan

- [ ] 1. Write bug condition exploration test
  - **Property 1: Bug Condition** - Configuration Validation and Command Execution Failure
  - **CRITICAL**: This test MUST FAIL on unfixed code - failure confirms the bug exists
  - **DO NOT attempt to fix the test or the code when it fails**
  - **NOTE**: This test encodes the expected behavior - it will validate the fix when it passes after implementation
  - **GOAL**: Surface counterexamples that demonstrate the bug exists
  - **Scoped PBT Approach**: For deterministic bugs, scope the property to the concrete failing case(s) to ensure reproducibility
  - Test that OpenClaw configuration with invalid `commands.native` format fails validation
  - Test that configuration with unrecognized `agents.defaults.tools` key fails validation
  - Test that command execution requests via Telegram fail to invoke shell tool
  - Test that shell tool is not registered in OpenClaw's tool registry
  - The test assertions should match the Expected Behavior Properties from design:
    - Configuration validation should pass (will fail on unfixed config)
    - Shell tool should be registered (will fail on unfixed config)
    - Command execution should work (will fail on unfixed config)
  - Run test on UNFIXED code
  - **EXPECTED OUTCOME**: Test FAILS (this is correct - it proves the bug exists)
  - Document counterexamples found to understand root cause:
    - Exact validation error messages
    - Missing tools in registry
    - Failed command execution attempts
  - Mark task complete when test is written, run, and failure is documented
  - _Requirements: 1.1, 1.2, 1.3, 1.4, 1.5_

- [ ] 2. Write preservation property tests (BEFORE implementing fix)
  - **Property 2: Preservation** - Non-Command Functionality Unchanged
  - **IMPORTANT**: Follow observation-first methodology
  - Observe behavior on UNFIXED code for non-buggy inputs (non-command requests)
  - Test cases to observe and capture:
    - Standard text queries (e.g., "What's the weather?") generate responses normally
    - Fallback models (e.g., "ollama/gpt-oss:latest") continue to function
    - Telegram features (message sending, channel management) operate correctly
    - Non-tool requests process without attempting shell execution
  - Write property-based tests capturing observed behavior patterns from Preservation Requirements:
    - For all non-command requests, response generation behavior is unchanged
    - For all fallback model requests, model functionality is unchanged
    - For all Telegram feature interactions, operations continue normally
    - For all non-tool requests, no shell tool invocation is attempted
  - Property-based testing generates many test cases for stronger guarantees
  - Run tests on UNFIXED code
  - **EXPECTED OUTCOME**: Tests PASS (this confirms baseline behavior to preserve)
  - Mark task complete when tests are written, run, and passing on unfixed code
  - _Requirements: 3.1, 3.2, 3.3, 3.4, 3.5_

- [ ] 3. Fix OpenClaw configuration for command execution

  - [ ] 3.1 Implement the configuration fix
    - Correct the `commands` configuration format to pass OpenClaw validation
    - Remove or relocate the unrecognized `agents.defaults.tools` key
    - Add explicit shell tool registration in the correct configuration section
    - Verify Gemma 4 model configuration supports tool invocation
    - Test configuration validation to ensure it passes without errors
    - Verify shell tool appears in OpenClaw's registered tools list
    - Confirm Gemma 4 can see and invoke the shell tool
    - _Bug_Condition: isBugCondition(config) where config.commands.native is invalid format OR config.agents.defaults.tools is unrecognized key AND shellToolNotRegistered() AND commandExecutionFails()_
    - _Expected_Behavior: configValidationPasses(config_fixed) AND shellToolRegistered(config_fixed) AND commandExecutionWorks(config_fixed) from design_
    - _Preservation: Non-command requests (text generation, fallback models, Telegram features, non-tool interactions) must produce exactly the same behavior as before the fix_
    - _Requirements: 1.1, 1.2, 1.3, 1.4, 1.5, 2.1, 2.2, 2.3, 2.4, 2.5, 3.1, 3.2, 3.3, 3.4, 3.5_

  - [ ] 3.2 Verify bug condition exploration test now passes
    - **Property 1: Expected Behavior** - Configuration Validation and Command Execution Success
    - **IMPORTANT**: Re-run the SAME test from task 1 - do NOT write a new test
    - The test from task 1 encodes the expected behavior
    - When this test passes, it confirms the expected behavior is satisfied
    - Run bug condition exploration test from step 1
    - **EXPECTED OUTCOME**: Test PASSES (confirms bug is fixed)
    - Verify configuration validation passes without errors
    - Verify shell tool is registered in OpenClaw's tool registry
    - Verify command execution requests successfully invoke shell tool
    - _Requirements: 2.1, 2.2, 2.3, 2.4, 2.5_

  - [ ] 3.3 Verify preservation tests still pass
    - **Property 2: Preservation** - Non-Command Functionality Unchanged
    - **IMPORTANT**: Re-run the SAME tests from task 2 - do NOT write new tests
    - Run preservation property tests from step 2
    - **EXPECTED OUTCOME**: Tests PASS (confirms no regressions)
    - Confirm all tests still pass after fix (no regressions):
      - Standard text queries continue to work
      - Fallback models continue to function
      - Telegram features continue to operate
      - Non-tool requests continue to process normally
    - _Requirements: 3.1, 3.2, 3.3, 3.4, 3.5_

- [ ] 4. Checkpoint - Ensure all tests pass
  - Ensure all tests pass, ask the user if questions arise.
